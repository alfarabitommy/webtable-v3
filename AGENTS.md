# AGENTS.md — Synapse (webtable)

**Synapse** — Indonesian, mobile-first AI GPU rental / GPUaaS investment platform: rent GPU nodes for daily ROI, single authoritative wallet ledger (`wallet_ledger`), multi-level agency/affiliate system, admin command center.

## Project
- Stack: CodeIgniter 3 (PHP 8.x), MySQL (`db_webtable`), Vanilla JS, Tailwind CSS + Font Awesome via CDN. No Composer runtime deps; no build step.
- Entry point: `index.php` (CI3 front controller). Clean URLs (`index_page=''`) via `application/config/routes.php`; Apache rewrite in `.htaccess`, nginx in `nginx-rewrite.txt`. `base_url` = `http://synapse.test/`.
- UI language: member-facing surfaces are **dual-language EN/ID** (plan/94 — `en` = default for new visitors, `id` secondary); the **admin panel stays 100% Indonesian** (invariant L1 — `Admin`/`Admin_auth` never call `i18n_apply()`). **All money is IDR only** (no foreign currencies at any layer) and is never localized — `Rp` + `number_format(id-ID)` in both idioms (L6).
- Business spec is authoritative in `docs/`: `1_PRD.md` (v5.0 — exact business logic), `2_ERD.md`, `3_ROADMAP.md` (v6.0 — strict agent rules), `4_UI_UX_GUIDELINES.md`.

## Commands
- Lint every new/modified PHP file (roadmap rule): `php -l <file>`
- No app test suite. Verify flows via browser/`curl` (HTTP 200 / 302 expected).
- Local dev: `php -S localhost:8080` from project root (pretty URLs need the rewrite config; default config expects `synapse.test`).
- DB credentials in `application/config/database.php` (env-driven via `DB_HOSTNAME`/`DB_USERNAME`/`DB_PASSWORD`/`DB_DATABASE`, no committed secrets); schema seed in `database.sql` — canonical DDL inventory: `users`, `gpu_products`, `bank_accounts`, `withdrawals`, `wallet_ledger`, `deposits`, `user_rentals`, `admins`, `user_notifications`, `promoter_claims`, `system_settings`, `system_audit_logs`, `rate_limits`. Retention-only legacy tables annotated `DEPRECATED (M10)` in the DDL: `rentals` (live = `user_rentals`), `otp_logs` (no OTP flow). `site_settings` and double-entry `transactions` are removed from canonical schema (M7/M6; see plan/68–69, plan/70–71).

## Architecture
- `application/core/MY_Controller.php` — base for user controllers: guards `user_id` session (redirect `login`), injects `global_balance`, `global_unread_count`, `global_notifications` into all views. Fixed statement order after `parent::__construct()`: **maintenance gate** (plan/95) → WIB tz pin (M2) → `i18n_apply()` (plan/94 F1) → login guard → lazy rental-expiry sweep (M3).
- Controllers: `Auth` (register/login/logout, native SVG CAPTCHA, phone normalizer), `Home`, `Rentals`, `Wallet` (deposit/withdraw/bind bank), `Marketplace`, `Team`, `Profile`, `Help`, `Notification` (AJAX), `User`, `Lang` (member dual-language switcher, `GET /lang/switch/(en|id)`, plan/94); **admin** = `Admin` + `Admin_auth` with cloaked login at `/control-panel` (route `'control-panel' => 'Admin_auth/login'`), guarded by `admin_id` session.
- Models: `User_model`, `Product_model`, `Rental_model`, `Wallet_model`, `Admin_model`, `Notification_model`, `Audit_model`, `Rate_limit_model`, `Promoter_model`. **All DB access lives in models** — no SQL in controllers/views. Domain config maps live in `application/config/`: `rebate_commission.php` (3-tier rebate fallback 1/5/3/1) & `promoter_rewards.php` (promoter reward tier `product_id → omzet_cost`).
- Ledger: `wallet_ledger` is the **sole authoritative transaction ledger** (immutable `credit`/`debit` rows keyed by deterministic `transaction_id`; the legacy `transactions` double-entry table was decommissioned in M6 — see `plan/68` audit & `plan/69` summary). All money moves run inside ACID transactions (`trans_begin/commit/rollback`, `SELECT ... FOR UPDATE` anchor on `users`); wallet balance = `SUM(credit) − SUM(debit)` on `wallet_ledger` (`Wallet_model::get_balance`).
- Views: user pages = `templates/header.php` + `templates/bottom_nav.php`; admin = `admin/templates/*`. Shared partials: `templates/lang_switcher.php` (auth pages only, plan/94), `templates/auth_theme_toggle.php` (auth pages only, plan/97), `views/errors/html/maintenance.php` (standalone 503, plan/95). Member header (plan/100) holds **exactly three groups — Brand, Balance Pill, Notification Bell**; Language/Theme controls live on the Profile page (`profile/index.php` §"App Preferences"). Mobile-first `max-w-[480px]` shell, Tailwind utilities.

## Conventions
- Every PHP file: `defined('BASEPATH') OR exit('No direct script access allowed');`
- SQL only in models, with bound params (`$this->db->query("... ?", [$id])`); query builder OK (see `Admin`).
- Dual-auth hard separation: `user_id` vs `admin_id` sessions — never shared; admin routes redirect to `control-panel` when `admin_id` missing.
- Phone normalization (backend is source of truth): strip non-digits, `62`/`0062` → `0`, final `/^0[0-9]{9,13}$/` (implemented as `_normalize_phone()` in `Auth` and `Admin`).
- Money display: PHP `number_format($val, 0, ',', '.')` with `Rp ` prefix; JS `Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 })`.
- **API responses (mandatory, M9/P7 — plan/76–77):** every AJAX/JSON endpoint responds through `application/helpers/api_helper.php` (`api_success()` / `api_error()`) with the canonical envelope `{success, message, data, code?, errors}` plus root legacy keys where the frontend still reads them — never hand-rolled `{success,...}` / `{error,...}` shapes. AJAX 404/error/exception responses are normalized to clean JSON by `application/core/MY_Exceptions.php` (`_wants_json()`), so `fetch().json()` never parses HTML.
- **Integer IDR discipline (mandatory, M8 — plan/74–75):** all monetary inputs validated server-side with `^[1-9][0-9]*$` before model execution; every credit/debit passes the single `(int)` choke-point + positivity assertion in `Wallet_model::_post()`; fee arithmetic uses `intdiv()` (never float division). MySQL retains `DECIMAL(15,2)` for non-breaking compatibility, but the authoritative balance is integer-typed: `get_balance()` / `lock_and_get_balance()` return `(int)`.
- **Framework compatibility rule (PHP 8.3):** the CI3 pagination root fix at `system/libraries/Pagination.php` (~`:526`, `(string)` cast so `ctype_digit(null)` is never hit when the `per_page` query segment is absent) is **required** on PHP 8.1+ and must never be reverted by a framework sync/upgrade (plan/80 follow-up). The same applies to the `User_model::LEVEL1_BONUS`/`WAGE_TIERS` const pattern — amounts are centralized, never re-inlined.
- Business state machines (deposit `pending→success`, single pending WD, WD window Mon–Sat 07:00–19:00, fee tiers) are in `docs/1_PRD.md` — read it before touching business logic.
- No hardcoded credentials/keys in repo (roadmap rule; use env vars). ✅ Phase 10D removed all committed secrets: Google-hosted CAPTCHA secret (`.htaccess`), `seeder_admin()` backdoor, `Test_core.php`; `base_url`, `encryption_key`, `log_threshold`, `proxy_ips` (`TRUSTED_PROXIES`), and DB credentials are now env-driven with dev fallbacks. M8 (plan/72–73) purged the external CAPTCHA service entirely — auth uses a native session-bound SVG CAPTCHA, so **no CAPTCHA keys exist anywhere** in code or env contract. Never re-commit secrets — see `plan/28_PHASE_10D_PLAN.md` §3 env contract.
- Work on per-phase branches per `docs/3_ROADMAP.md`; commit messages in Indonesian (existing style).
- Register pretty URLs in `application/config/routes.php` (add a `$route[...]`), not as controller method URLs.

## Notes
<!-- quick-add notes -->
- M8 (plan/72–73) auth CAPTCHA is a **native inline-SVG challenge**, fully replacing the external Google-hosted CAPTCHA: engine in `application/helpers/captcha_helper.php` (`captcha_alphabet()` = 56 unambiguous glyphs, `build_captcha_svg()` = transparent SVG w/ rotation+jitter+noise, indigo/cyan/violet palette); lifecycle lives in `Auth` (`auth_captcha` session data, **TTL 180 s**, **strict single-use flush on every evaluation**, `_render_auth_view()` re-issues a fresh challenge on every render). Refresh endpoint: GET `auth/refresh_captcha` (AJAX-only, JSON `{svg, expires_in}`), no `routes.php` entry needed (direct method URL). Case-insensitive compare; failure message "Kode keamanan salah atau sudah kedaluwarsa."
- M3 (plan/60–61) rental expiry is LAZY/event-driven, no cron: `MY_Controller` runs `Rental_model::expire_user_rentals()` on every authenticated request (after tz/session init); all "active rental" eligibility SQL filters `expired_at > <PHP WIB bound param>` (never MySQL `NOW()`); authoritative gate stays `Rental_model::claim_roi()` (C2). Manual tooling: `scripts/expire_rentals.php` (`--dry-run`/`--apply`) + admin POST `admin/expire_expired_rentals` (dashboard "Tutup Sewa Kedaluwarsa"). Indexes on `user_rentals`: `idx_user_status_expired(user_id,status,expired_at)`, `idx_status_expired(status,expired_at)` — live DB + `database.sql` in sync.
- Audit closure (plan/82): all **24 audit findings (C1–C7, M1–M10, P1–P7) are CLOSED & VERIFIED — 100%**. Canonical schema list above re-verified against `database.sql` at closure (`rentals`/`otp_logs` retention-only `-- DEPRECATED (M10)`; no `site_settings`/`transactions`). Definitive matrix, architectural invariants, and the production handoff checklist (DB password rotation, `ENVIRONMENT='production'`, payment-gateway integration) live in `plan/82_FINAL_SYSTEM_STABILIZATION_AND_AUDIT_CLOSURE.md`; gap ledger closure in `plan/66`; per-finding resolution index in `plan/37` §0.
- Rebate & promoter program (plan/89–92, **100% runtime verified**, sign-off `dec-94563cfd1af2c22e`; ERD/ROADMAP synced per plan/93): 3-tier purchase rebate — `Rental_model::_distribute_rebate()` runs inside the `checkout_rental()` TX (step 7, before commit): active upline L1/L2/L3 receive credits keyed `RBT-{rental_id}-L{tier}` via the **sole** `Wallet_model::credit()` path (Z1/C4); rates are dynamic `system_settings` keys `rebate_enabled`/`rebate_l1_percent`/`rebate_l2_percent`/`rebate_l3_percent` (default 1/5/3/1) with `application/config/rebate_commission.php` fallback; breakage = inactive/banned upline hangs (no pass-up); referral gating Condition A/B/C hides invite code/link/QR in DOM until rental criteria met (except promoters — see K1). Promoter omzet-burn — `Promoter_model` TX lock order is always `users FOR UPDATE` → `promoter_claims FOR UPDATE` (C5); all status flips are conditional `WHERE status='pending'` with `affected_rows()===1` (M4); approve mints a zero-cost contract row, audit + notification atomically in the same TX (M5).
- **K4 — independent channel caps (plan/91–92):** `user_rentals.source` ENUM('purchase','promoter_reward') separates the two channels; per-user `max_per_user` quota is enforced **per channel** — `promoter_reward` contracts never consume the paid-purchase quota (GATE 2 `checkout_rental` and `Product_model` marketplace quota both filter `source <> 'promoter_reward'`; the reward channel has its own quota check in `Promoter_model` submit/approve).
- **K7 — zero-cost contract properties (plan/91–92):** approved reward contracts are normal active `user_rentals` rows with `source='promoter_reward'` and `purchase_price=0` — ROI is claimable via the existing `ROI-{rental_id}-D…` path and they qualify the holder for rebate eligibility, but issuance triggers **no** `_distribute_rebate` and adds **nothing** to upline omzet (Σ price = 0); omzet burn is pure `promoter_claims` bookkeeping and never writes `wallet_ledger` (Z1). `is_promoter` is admin-only (audited `admin_toggle_promoter`); the flag is read fresh inside the submit TX so demotion instantly blocks new claims while pending ones stay queued (K6).
- plan/95–96 (member maintenance mode; summary `plan/96_MAINTENANCE_MODE_SUMMARY.md`): key `system_settings.is_maintenance_mode` (`'0'`=normal, `'1'`=locked; **default OFF bila baris hilang** — beda dari `is_registration_open`); gate engine `application/helpers/maintenance_helper.php` (`maintenance_is_active()`/`maintenance_gate()`/`_maintenance_wants_json()`, autoloaded) dipanggil sebagai **statement pertama setelah `parent::__construct()`** di `MY_Controller`, `Auth`, dan `Lang` — sebelum pin WIB M2, `i18n_apply`, guard login, dan sweep rental M3. Bypass ketat: admin routes/`control-panel` (constructor tidak memanggil gate) + session `admin_id` + `is_cli()`. Saat aktif: HTML → `views/errors/html/maintenance.php` HTTP 503 (standalone, dwibahasa, no-store, tanpa shell member); JSON/AJAX → `api_error(..., 503, [], 'MAINTENANCE_MODE')` (deteksi parity `MY_Exceptions::_wants_json`). Admin toggle: `POST /admin/toggle-maintenance` → `Admin::toggle_maintenance()` (POST-only M4, CSRF, TX atomik + audit `admin_toggle_maintenance` `{was_maintenance,is_maintenance}`), tombol dashboard berdampingan dgn "Toggle Pendaftaran" (subtle saat normal, ring merah/pulse saat aktif).
- **plan/94 (F1 — Dual-Language Engine, member-only; summary `plan/94_DUAL_LANGUAGE_AND_ADMIN_ALERT_CENTER_SUMMARY.md`):** engine `application/helpers/i18n_helper.php` (`i18n_idioms()` = peta satu-sumber `en→english`/`id→indonesian`, `i18n_default_code()` = `en`, `i18n_resolve()` = **session `site_lang` → cookie `site_lang` (30 hari) → `en`**, `i18n_apply()` memuat **tepat satu** idiom + inject var `site_lang_code`, `i18n_is_referer_same_host()` anti open-redirect L8) — semua fungsi dibungkus `function_exists()`. Switcher `GET /lang/switch/(:any)` → `Lang::switch()` (kode ∉ {en,id} → 404; set session + `set_cookie('site_lang', 86400*30)`; redirect referer same-host, fallback `base_url()`); route `$route['lang/switch/(:any)'] = 'lang/switch/$1'`. Ketersediaan pra-login (dipakai juga oleh auth views via `templates/lang_switcher.php`). Kamus `application/language/{english,indonesian}/app_lang.php` — **332 key, himpunan key identik** (paritas diuji `sort`+`diff` di plan/100). Invariant: admin **tidak pernah** memanggil `i18n_apply()` (L1); teks JS dinamis lewat `window.SYNAPSE_I18N`; uang/angka **tidak** diterjemahkan (L6).
- **plan/94 (F2 — Admin Real-time Alert Center; summary `plan/94_DUAL_LANGUAGE_AND_ADMIN_ALERT_CENTER_SUMMARY.md`):** endpoint `GET /admin/alerts/poll` → `Admin::alerts_poll()` (`$route['admin/alerts/poll'] = 'admin/alerts_poll'`), mengembalikan `Admin_model::get_alert_counts()` (3× `COUNT(*) WHERE status='pending'` pada deposits/withdrawals/promoter_claims + `total_urgent`) via `api_success(..., 200, $counts)` — envelope `data.*` **plus** root legacy keys — dengan header `Cache-Control: no-store`; read-only, **tanpa** baris audit (A1). Klien di `views/admin/templates/footer.php` (`window.AdminAlerts`): **polling 25 dtk** (`POLL_MS = 25000`), in-flight guard, pause saat `document.hidden` + poll langsung saat visible, sinkronisasi badge sidebar/bell dari SSR `global_admin_alerts` (di-inject di constructor `Admin`), **chime Web Audio 2 nada 880 → 1174.66 Hz** hanya saat `total_urgent` **naik** (bukan tiap poll; AudioContext baru dibuat/resume pada user gesture pertama), mute persist `localStorage('admin_alerts_muted')`, reload saat sesi mati (≥2 kegagalan beruntun). Deep-link: `/admin#pending-deposits`, `/admin#pending-withdrawals` (`scroll-mt-24`), `/admin/promoter-claims`. Index komposit `INDEX idx_status_created (status, created_at)` **kanonik** di `CREATE TABLE withdrawals` (`database.sql:125`), `deposits` (`:184`), dan `promoter_claims` (`:262`), plus catatan migrasi live one-time di `database.sql:378–390` (MySQL 8 `ALTER … ADD INDEX`; MariaDB `CREATE INDEX IF NOT EXISTS`) — melayani COUNT pending (leading `status`) sekaligus listing `ORDER BY created_at ASC`.
- **plan/97–98 (Auth UI — Wordmark & High-Tech Animated Background; summaries `plan/97_...SUMMARY.md`, `plan/98_...SUMMARY.md`):** murni **view + CSS + JS vanilla** — nol perubahan controller/model/skema/i18n. Ketiga view auth (`views/auth/{login,register,change_password}.php`) memakai: **wordmark vektor murni (SVG `SYNAPSE`)** — mark neural 7-node + `<h1 class="u-auth-wordmark">` gradient `background-clip:text` + `letter-spacing .3em` (tanpa aset gambar; `placehold.co` dan seluruh `<img>` dihapus); **grid sirkuit siber high-tech murni CSS/SVG** sebagai ambient background: 5 plasma core (blur **statis** 35–50 px, animasi transform-only 7–11 s, `mix-blend-mode: screen` di dark), satu inline SVG `viewBox="0 0 480 900"` (`preserveAspectRatio="xMidYMid slice"` — grid pattern 28 px, signal ring dashed 12 s, 2 GPU die + 24 pin, 7 trace statis, node) dan **3 bus data** (kiri cyan / kanan violet / bawah biru-teal), tiap bus = `t-bus` + `p-dots` + `p-burst` + `p-tail` yang dianimasikan `stroke-dashoffset` linear **160 px/s seragam** (Δ kelipatan periode; T = 6 s / 7 s / 8 s → `−960px` / `−1120px` / `−1280px`, tail tertinggal tepat 40 px); token `--u-*` dwibahasa tema (dark kanvas `#050811` cyan/violet vivid, light `#f8fafc` indigo halus) + `prefers-reduced-motion: reduce` freeze plasma/pulse/node/ring. Kartu `auth-card` glass (backdrop-blur 16 px), form, CAPTCHA, dan `templates/auth_theme_toggle.php` (toggle Sun/Moon, parity `localStorage['user_theme']`) **tidak tersentuh** oleh plan/98; nol aset eksternal baru (hanya Tailwind Play CDN).
- **plan/99 + 101 (Member Dashboard — AI Neural Cluster Hero & World Node Map; summaries `plan/99_...SUMMARY.md`, `plan/101_...SUMMARY.md`):** satu file `application/views/home/index.php` (228 → 465 → 525 baris), murni view + 12 key kamus baru (`home_hub_*`, `home_hud_*`, `home_topology_*`) — nol perubahan controller/model/routes. **Hero card** `.hm99-hero`: kanvas obsidian `#0b1120` (dark) / pearl (light), 2 orb glow, neural dot-lattice `::before` + sheen `::after`, headline gradient-text, status dot pulsing, dan **3 micro-HUD pill** (PFLOPS aktif via `fa-bolt`, Isolated via `fa-lock`, Direct Link via `fa-globe`). **World Node Map** `.hm99-map`: header judul `home_topology_title` + chip SLA, SVG `viewBox="0 0 1000 500"` (`width:100%; height:auto` — rasio intrinsik 2:1, **tanpa letterbox** agar overlay label `%` tetap segaris node di 360–480 px) berisi graticule → **kontur benua vektor tajam** hasil Q-midpoint smoothing (**6 `hm99-land`**, bukan lagi ellipse + `feGaussianBlur`) → **14 ring pulau `hm99-isle`** (Indonesia/ASEAN: Sumatra, Java, Borneo, Sulawesi, New Guinea + Jepang Honshu/Hokkaido, Filipina Luzon/Mindanao, Greenland, UK/IE, Islandia, Madagaskar) → **8 backbone transit link `hm99-bb`** (dashed statis) → **5 rute aktif `hm99-route`** dengan **5 pulsa `hm99-flow`** (`pathLength="1"`, `dasharray .04 .96`, `stroke-dashoffset → −1`, tepat satu komet per loop) → **5 edge node `hm99-edge`** (halo + core, breathing opacity 4 s) → 5 ping ring + core hub → 5 label hub HTML overlay (FRA-1, US-EAST, TYO-1, SIN-1, JKT-2). **Nol filter SVG** (`<defs>`/`feGaussianBlur`/`filter=url()` dihapus total — landmass ter-composite sekali, tanpa re-raster per frame); semua warna lewat kelas CSS berpasangan `html.dark .hm99-*` (tanpa atribut warna inline); animasi hanya `stroke-dashoffset`/`transform`/`opacity`/`box-shadow` (60 FPS) dan dimatikan oleh `prefers-reduced-motion: reduce` (blanket `.hm99-map *`).
- **plan/100 (Header Declutter & Profile "App Preferences"; summary `plan/100_HEADER_DECLUTTER_AND_PROFILE_SETTINGS_SUMMARY.md`):** `views/templates/header.php` (464 → 430 baris) kembali ke **tepat 3 grup — Brand (logo + wordmark) · Balance Pill · Notification Bell**; `templates/lang_switcher.php` include dan blok `#user-theme-toggle` **dihapus** dari header, begitu pula engine tema `toggleUserTheme()`/`syncThemeUI()` + key `SYNAPSE_I18N` `js_theme_dark`/`js_theme_light`. Balance pill diperkuat **tanpa perubahan perilaku**: `min-w-0 max-w-[46vw]` pada anchor + `flex-shrink-0` ikon + `truncate` pada span amount + `title="Rp {nominal penuh}"` (di 360 px pill ≤ ~166 px, tidak pernah menabrak bell; nominal penuh tetap terbaca via tooltip). Kontrol bahasa & tema **direlokasi kanonik** ke kartu **"App Preferences / Pengaturan Tampilan & Bahasa"** di `application/views/profile/index.php` (368 → 480 baris, disisipkan antara REFERRAL CENTER dan THE HUB): Row 1 Bahasa (segmented capsule EN/ID → anchor `lang/switch/en|id`, segmen aktif server-rendered dari `$site_lang_code`), Row 2 Tema (segmented `#pref-theme-seg` dua `button.pref-theme-opt`, `aria-pressed` via JS) + IIFE berguard `window.__profilePrefInit` yang menulis `localStorage['user_theme']` dan dispatch `CustomEvent('user-theme-change')` (parity kontrak engine lama); baris hub `#btn-theme-hub` lama **dihapus** dan penomoran hub jadi 1–6. Kamus: +6 key (`profile_pref_title`, `profile_lang_label`, `profile_theme_label`, `profile_theme_dark`, `profile_theme_light`, `profile_theme_hint`), −3 key usang (`profile_theme`, `js_theme_dark`, `js_theme_light`) → **332 key** di kedua idiom. Tema/bahasa halaman **auth** (plan/97–98) dan toggle admin tetap utuh — di luar scope plan/100.
